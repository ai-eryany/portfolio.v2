import LoaderElement from './Loader.element';
import FontLoaderElement from './FontLoader.element';

const Loader = {
  LoaderElement,
  FontLoaderElement,
};

export default Loader;
export { LoaderElement, FontLoaderElement };
