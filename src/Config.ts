const meta = {
  appTag: 'portfolio',
  defaultLang: 'en',
  appName: 'portfolio',
};

const globalAnimationDelay = 200;

const Config = {
  meta,
  globalAnimationDelay,
};

export default Config;
